# TypeScript Types

This directory contains shared TypeScript types that are used across the frontend application.

Types should be organized by domain (e.g., `auth.ts`, `chat.ts`, `dashboard.ts`) and can be shared with the backend team for consistency.
